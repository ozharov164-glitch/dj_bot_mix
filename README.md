# FADELINE Mini App

Standalone Telegram Mini App for GitHub Pages. Backend stays on the VPS — never push API/bot/worker here.

```bash
pnpm install
pnpm dev
```

Set repository variable `VITE_API_URL` to the public HTTPS API origin before production Pages deploy.

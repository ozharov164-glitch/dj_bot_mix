# Public API origin for the Mini App build (not a secret)
VITE_API_URL=http://localhost:3000

# Optional: override Vite base path (default / locally; /dj_bot_mix/ on GitHub Actions)
# VITE_BASE_PATH=/

# Public API origin for local Mini App development (not a secret)
VITE_API_URL=http://localhost:3000

# Optional: allow localhost API during production-mode CI builds only
# VITE_ALLOW_DEV_API=true

# Optional: override Vite base path (default / locally; /dj_bot_mix/ on GitHub Actions)
# VITE_BASE_PATH=/
